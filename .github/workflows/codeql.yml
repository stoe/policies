name: CodeQL

on:
  push:
    branches:
      - main
    paths-ignore:
      - '*.md'
  pull_request:
    branches:
      - main
    paths-ignore:
      - '*.md'

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  codeql:
    uses: stoe/policies/.github/workflows/policy-node-codeql.yml@main
